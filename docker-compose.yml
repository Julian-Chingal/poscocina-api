name: poscocina-api

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3001:3000'
    env_file:
      - ${ENV_FILE:-.env}
    environment:
      RUN_MIGRATIONS: 'false'
      RUN_SEED: 'false'
